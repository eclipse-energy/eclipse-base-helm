{{- if .Values.core.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-core
  {{- include "marketplace.core.labels" . | indent 2 }}
spec:
  selector:
    app: {{ .Release.Name }}-core
  ports:
    - name: marketplace
      targetPort: marketplace
      port: 8080
{{- end }}