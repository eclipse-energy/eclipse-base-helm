apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-core
  {{- include "federator.core.labels" . | indent 2 }}
spec:
  selector:
    app: {{ .Release.Name }}-core
  ports:
    - name: federator
      targetPort: federator
      port: 8080