{{- if eq .Values.jaeger.enabled true }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name}}-grafana
spec:
  selector:
    app: {{ .Release.Name }}-grafana
  ports:
    - name: grafana
      port: 3000
      targetPort: grafana
{{- end }}
