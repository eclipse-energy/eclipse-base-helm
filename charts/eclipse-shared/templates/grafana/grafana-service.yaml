{{- if eq .Values.jaeger.enabled true }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name}}-grafana
spec:
  selector:
    app: grafana # .Release.Name would only be supported with various overrides
  ports:
    - name: grafana
      port: 3000
      targetPort: grafana
{{- end }}
