{{- if eq .Values.core.enabled true }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-core-secret
type: Opaque
stringData:
  mqttUsername: {{ .Values.mqtt.username | quote }}
  mqttPassword: {{ .Values.mqtt.password | quote  }}
{{- end }}