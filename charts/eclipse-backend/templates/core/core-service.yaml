{{- if eq .Values.core.enabled true }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-core
spec:
  selector:
    app: {{ .Release.Name }}-core
  ports:
    - name: backend
      targetPort: backend
      port: 8080
{{- end }}